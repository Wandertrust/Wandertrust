<div class="d-flex justify-content-center">
    <%= image_tag("wandertrust_logo.png", width: "500") %>
</div>
<div class="text-center">
  <h6>Start tracking your adventures so you can trust where you wander...</h6>
  <br>
  <%if current_user%>
    <a class="btn btn-center btn-dark btn-md" href=<%=new_user_trip_path(current_user.id)%>>🏔️ let's wander 🏔️</a>
  <%else%>
    <a id="register-button" class="btn btn-center btn-dark btn-md" href=<%=register_path%>>🏔️ let's wander 🏔️</a>
  <%end%>
</div>