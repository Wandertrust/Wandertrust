<!DOCTYPE html>
<html>
  <head>
    <title>Wandertrust</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <%= link_to 'Wandertrust', root_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item justify-content-end">
            <%if !current_user%>
              <%= link_to 'register', register_path, class: "nav-link" %>
            <%end%>
          </li>
          <li class="nav-item justify-content-end">
            <%if !current_user%>
              <%= link_to 'login', login_path, class: "nav-link" %>
            <%end%>
          </li>
          <li class="nav-item justify-content-end">
            <%if current_user%>
              <%= button_to '👋 logout', '/login', method: :delete, class: "nav-link" %>
            <%end%>
          </li>
          <li class="nav-item justify-content-end">
            <%if current_user%>
              <%= button_to '🏔️ my dashboard', user_path(current_user.id), method: :get, class: "nav-link" %>
            <%end%>
          </li>
          <li class="nav-item justify-content-end">
            <%if current_user%>
              <%= button_to '📍 new adventure', new_user_trip_path(current_user.id), method: :get, class: "nav-link" %>
            <%end%>
          </li>
        </ul>
      </div>
    </div>
  </nav>

    <%= yield %>
  </body>
</html>
